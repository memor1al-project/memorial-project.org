# Prevent secrets from being committed. One-time setup per dev machine:
#   brew install pre-commit  # or: pip install pre-commit
#   pre-commit install
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
